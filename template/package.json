{
  "name": "{{ name }}",
  "version": "{{ version }}",
  "description": "{{ description }}",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && NODE_ENV=production node index.js",
    "build:android": "PLATFORM=android npm run build",
    "build:ios": "PLATFORM=ios npm run build",
    "clean": "rimraf dist",
    "start": "node index.js",
    "start:android": "PLATFORM=android npm run start",
    "start:ios": "PLATFORM=ios npm run start"
  },
  "dependencies": {
    "nativescript-vue": "1.0.0",
    "tns-core-modules": "3.4.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.1",
    "css-loader": "^0.28.10",
    "fs-extra": "^5.0.0",
    "nativescript-vue-loader": "^0.1.5",
    "nativescript-vue-target": "^0.1.0",
    "nativescript-vue-template-compiler": "^1.2.0",
    "rimraf": "^2.6.2",
    "vue-template-compiler": "^2.5.13",
    "webpack": "^3.11.0",
    "winston-color": "^1.0.0"
  },
  "author": "{{ author }}",
  "license": "{{ license }}"
}
